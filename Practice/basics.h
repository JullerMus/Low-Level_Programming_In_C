#ifndef BASICS_H
#define BASICS_H


void reverse_string(char input[]);
int is_palindrome(const char *s);
void count_lines_words_chars();

char *reverse_string2(char input[]); //pointer to string, because C cannot return arrays.


#endif // BASICS
